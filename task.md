tôi cần chuyển hệ thống deutschtiger sang flutter cho cả ios và android. Hiện tại toàn bộ codebase ở /Users/quangcuong/Desktop/Deutsch -> tôi muốn có bản v1 đầu tiên để appstore kiểm duyệt trên ios và ch play. dựa vào recommend.md để xây dựng v1.
Hãy nhìn vào thư mục để recommend cấu trúc cho tôi hợp lý hơn.
Hiện tại website đang bị 1 cái lỗi cả backend và frontend cùng 1 chỗ (tiện cho AI sử dụng code) nhưng sang flutter thì cần xem xét codebase.
Mục tiêu của app là sau nhiều phiên bản sẽ được như website hiện tại